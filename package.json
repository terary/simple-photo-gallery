{
  "name": "typescript-express-tutorial",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean:old": "rm -rf dist && mkdir ./dist && cp -rf frontend-photo-gallery-client/build ./dist/client",
    "build": "npm run clean:old && tsc",
    "start": "node ./dist/main.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:bromix/typescript-express-tutorial.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/express": "^4.17.13",
    "typescript": "^4.3.5"
  },
  "dependencies": {
    "express": "^4.17.1"
  }
}
